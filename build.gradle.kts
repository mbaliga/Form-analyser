// Root build for Form Analyser (codename). Sport-specific archery code lives in
// :archery-module and depends on the sport-agnostic Crocodyl engine.
group = "xyz.mdhv.formanalyser"
version = "0.1.0"
