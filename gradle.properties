# Use the AndroidX libraries (required by the Compose/AndroidX deps in :app-android).
android.useAndroidX=true
android.nonTransitiveRClass=true

# JVM memory for the Gradle workers (engine/archery + the Android build).
org.gradle.jvmargs=-Xmx2048m -Dfile.encoding=UTF-8
kotlin.code.style=official
