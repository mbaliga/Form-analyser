name: android

on:
  push:
    branches: [main]
    paths:
      - "engine/**"
      - "archery-module/**"
      - "app-android/**"
      - "*.gradle.kts"
      - ".github/workflows/android.yml"
  pull_request:
    paths:
      - "engine/**"
      - "archery-module/**"
      - "app-android/**"
      - "*.gradle.kts"
      - ".github/workflows/android.yml"

jobs:
  assemble:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "21"
      # ubuntu-latest ships an Android SDK; this ensures cmdline-tools + accepted licenses
      # so AGP can fetch any missing platform/build-tools.
      - uses: android-actions/setup-android@v3
        with:
          # setup-android's default package set also requests the obsolete 'tools' package, which
          # is no longer in the SDK repository — sdkmanager exits 1 and the job dies before any
          # compilation. Ported from feat/immersive-visuals 0d7eefa, where CI is green with it.
          packages: platform-tools
      # -PwithAndroid opts :app-android into the build (see settings.gradle.kts).
      - name: Assemble debug APK
        run: ./gradlew :app-android:assembleDebug -PwithAndroid --no-daemon
      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: form-analyser-debug
          path: app-android/build/outputs/apk/debug/*.apk
          if-no-files-found: ignore
