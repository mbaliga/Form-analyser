<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base activity theme; Compose drives the real Hyle theming. Window sits at the token
         contract's field.near (#0A0809) — UI surfaces never pure black (halation rule). -->
    <style name="Theme.FormAnalyser" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">#0A0809</item>
        <item name="android:statusBarColor">#0A0809</item>
        <item name="android:navigationBarColor">#0A0809</item>
    </style>
</resources>
